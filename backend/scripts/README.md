# Backend Scripts

이 디렉토리는 데이터베이스 설정 및 유틸리티 스크립트를 포함합니다.

## 📁 디렉토리 구조

```
scripts/
├── migration/          # 데이터 마이그레이션 스크립트 (일회성)
│   └── migrate_to_postgresql.py
└── setup/             # 데이터베이스 설정 스크립트
    ├── check_data.py
    ├── create_audit_table.py
    └── create_draft_table.py
```

## 🔧 migration/ - 마이그레이션 스크립트

### `migrate_to_postgresql.py`
SQLite에서 PostgreSQL로 데이터를 마이그레이션하는 스크립트입니다.

**용도**: 일회성 실행 (이미 완료됨)
**상태**: ✅ 완료 - 보관용

```bash
# 실행 방법 (필요시)
python scripts/migration/migrate_to_postgresql.py
```

## 🛠️ setup/ - 설정 스크립트

### `check_data.py`
데이터베이스의 카테고리 및 제품 데이터를 확인하는 스크립트입니다.

**용도**: 데이터 검증
**사용 시점**: 데이터베이스 내용 확인이 필요할 때

```bash
python scripts/setup/check_data.py
```

### `create_audit_table.py`
Audit Log 테이블을 생성하는 스크립트입니다.

**용도**: audit_logs 테이블 생성
**상태**: 선택사항 (Audit 기능 사용 시 필요)

```bash
python scripts/setup/create_audit_table.py
```

### `create_draft_table.py`
Draft Products 테이블을 생성하는 스크립트입니다.

**용도**: draft_products 테이블 생성
**상태**: 선택사항 (Draft 기능 사용 시 필요)

```bash
python scripts/setup/create_draft_table.py
```

## 📌 운영 스크립트 (루트 디렉토리)

다음 스크립트들은 정기적으로 사용되므로 backend 루트에 유지됩니다:

- **`create_tables.py`**: 메인 테이블 생성 (필수)
- **`dummy_data.py`**: 더미 데이터 생성 (개발/테스트용)

## 🚀 새 환경 설정 순서

새로운 환경에서 데이터베이스를 설정할 때는 다음 순서로 실행하세요:

```bash
# 1. 메인 테이블 생성
python create_tables.py

# 2. (선택) Audit 테이블 생성
python scripts/setup/create_audit_table.py

# 3. (선택) Draft 테이블 생성
python scripts/setup/create_draft_table.py

# 4. (개발환경) 더미 데이터 생성
python dummy_data.py

# 5. 데이터 확인
python scripts/setup/check_data.py
```

## ⚠️ 주의사항

- `migration/` 폴더의 스크립트는 **이미 실행 완료**되었으므로 다시 실행하지 마세요.
- 프로덕션 환경에서는 `dummy_data.py`를 실행하지 마세요.
- 스크립트 실행 전 반드시 `.env` 파일이 올바르게 설정되어 있는지 확인하세요.
