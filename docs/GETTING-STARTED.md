# 🚀 처음 사용하시는 분을 위한 초기 설정 가이드

친구의 안전용품 회사를 위해 이 웹사이트를 설정하는 방법을 단계별로 안내합니다.

## ✅ 사전 요구사항

- Docker & Docker Compose 설치
- 회사 로고 및 제품 이미지 준비
- 제품 데이터 Excel 준비 (선택)

## 📋 Step 1: 프로젝트 다운로드 및 실행

```bash
# 1. 프로젝트 폴더로 이동
cd boram_safety

# 2. 환경 변수 설정
cp .env.example .env

# 3. .env 파일 수정 (필요시)
# DB_PASSWORD를 원하는 비밀번호로 변경

# 4. Docker Compose로 실행
docker-compose up -d

# 5. 데이터베이스 초기화
docker-compose exec backend python create_tables.py

# 6. 웹사이트 접속 확인
# http://localhost:3000 - 공개 사이트
# http://localhost:3000/admin - 관리자 페이지
```

## ⚙️ Step 2: 회사 정보 설정 (가장 중요!)

이제 친구의 회사 정보로 바꿔야 합니다!

### 2-1. 관리자 페이지 접속
```
http://localhost:3000/admin
```

### 2-2. 사이트 설정 페이지로 이동
- 대시보드 오른쪽 아래 **"⚙️ 사이트 설정"** 카드 클릭

### 2-3. 회사 정보 입력

#### 필수 항목
- **회사명**: 친구의 회사 이름 (예: "안전플러스")
- **슬로건**: 회사 모토 (예: "안전한 작업환경의 시작")

#### 권장 항목
- **전화번호**: 고객이 연락할 수 있는 전화번호
- **이메일**: 문의 받을 이메일 주소
- **주소**: 회사 소재지
- **영업시간**: "평일 09:00 - 18:00"
- **대표자명**: 대표님 성함

#### 선택 항목
- 팩스, 사업자등록번호
- 회사 소개 내용
- 소셜 미디어 링크 (Facebook, Instagram 등)

### 2-4. 저장
- **"설정 저장"** 버튼 클릭
- 성공 메시지 확인

## 📊 Step 3: 제품 데이터 업로드

### 방법 1: Excel로 일괄 업로드 (추천)

```
http://localhost:3000/admin/excel
```

#### 3-1. 템플릿 다운로드
- **"📄 템플릿 다운로드"** 버튼 클릭
- `product_template_YYYYMMDD.xlsx` 파일 받기

#### 3-2. Excel 파일 작성

```excel
카테고리코드      제품명              모델번호        가격
safety_helmet    산업용 안전모 A형   SH-2024-001    25000
safety_gloves    방한 안전장갑 특대  SG-2024-015    12000
safety_boots     고무 안전화 260mm   SB-2024-032    85000
```

**사용 가능한 카테고리 코드:**
- `safety_helmet` - 안전모
- `safety_gloves` - 안전장갑
- `safety_boots` - 안전화
- `safety_belt` - 안전벨트
- `protective_clothing` - 보호복
- `respiratory_protection` - 호흡보호구
- `hearing_protection` - 청력보호구
- `face_protection` - 안면보호구
- `leg_protection` - 다리보호구
- `musculoskeletal_protection` - 근골격계보호구
- `others` - 기타

#### 3-3. 업로드
- **"추가 모드"** 선택 (기존 데이터 유지)
- Excel 파일 선택
- **업로드** 버튼 클릭
- 결과 확인 (성공/실패 건수)

### 방법 2: 웹에서 하나씩 추가

```
http://localhost:3000/admin/products/new
```

- 제품명, 모델번호, 가격 등 입력
- 이미지 업로드
- **저장** 버튼 클릭

## 🖼️ Step 4: 제품 이미지 업로드

### 이미지 파일 준비
```
제품 이미지는 다음 규칙으로 준비:
- 파일명: 간단하고 의미있게 (예: helmet_001.jpg)
- 형식: JPG, PNG
- 크기: 가로 800px 권장
```

### 이미지 업로드 방법

#### 방법 1: 제품 추가/수정 시 업로드
- 제품 추가/수정 페이지에서 "이미지 선택" 버튼
- 이미지 파일 선택
- 저장하면 자동으로 업로드됨

#### 방법 2: 수동으로 서버에 복사
```bash
# Docker 컨테이너에 이미지 복사
docker cp ./images/helmet_001.jpg boram_safety-backend-1:/app/static/images/safety_helmet/

# 여러 파일 한번에 복사
docker cp ./images/* boram_safety-backend-1:/app/static/images/safety_helmet/
```

## 🎨 Step 5: 웹사이트 확인

### 5-1. 공개 사이트 확인
```
http://localhost:3000
```
- 회사명이 제대로 표시되는지 확인
- 제품 카테고리가 보이는지 확인
- 제품 클릭하여 상세 페이지 확인

### 5-2. 모바일 화면 확인
- 브라우저에서 F12 → 모바일 뷰 전환
- 반응형 디자인 확인

## 🔄 Step 6: 데이터 백업 (중요!)

### 정기적으로 백업하세요

#### 제품 데이터 백업
```
http://localhost:3000/admin/excel
→ "전체 제품 내보내기" 클릭
→ Excel 파일 저장
```

#### 데이터베이스 백업
```bash
# PostgreSQL 데이터베이스 덤프
docker-compose exec db pg_dump -U boramsafety_user boramsafety_db > backup_$(date +%Y%m%d).sql

# 복원 시
docker-compose exec -T db psql -U boramsafety_user boramsafety_db < backup_20241111.sql
```

## 📱 Step 7: 프로덕션 배포 (실제 운영)

### 도메인 연결 시

#### 7-1. 환경 변수 수정
```bash
# .env 파일 수정
NEXT_PUBLIC_API_URL=https://your-domain.com
```

#### 7-2. Docker Compose 재시작
```bash
docker-compose down
docker-compose up -d --build
```

#### 7-3. Nginx 설정 (선택)
- 자세한 내용은 [docs/DEPLOYMENT.md](DEPLOYMENT.md) 참조

## 🆘 문제 해결

### Q: 로그인이 없는데 관리자는 어떻게 보호하나요?
**A:** 현재는 단순 전시 사이트이므로 복잡한 인증이 없습니다. 필요시:
- Nginx로 `/admin` 경로에 Basic Auth 설정
- 특정 IP만 접속 허용
- 간단한 비밀번호 추가

### Q: 이미지가 안 보여요
**A:** 다음을 확인하세요:
- 이미지 파일이 `backend/static/images/{category}/` 경로에 있는지
- 파일명이 제품의 `file_path`와 일치하는지
- 파일 권한이 올바른지 (`chmod 644`)

### Q: Excel 업로드가 실패해요
**A:** 다음을 확인하세요:
- 카테고리코드가 정확한지 (예: `safety_helmet`)
- 필수 컬럼(카테고리코드, 제품명)이 있는지
- 가격이 숫자로만 입력되었는지
- 파일 크기가 10MB 이하인지

### Q: Docker 컨테이너가 안 떠요
**A:** 다음을 시도하세요:
```bash
# 로그 확인
docker-compose logs

# 컨테이너 재시작
docker-compose restart

# 완전히 재빌드
docker-compose down
docker-compose up -d --build
```

### Q: 데이터가 날아갔어요!
**A:** 백업이 있다면:
```bash
# Excel에서 복원
http://localhost:3000/admin/excel
→ "교체 모드"로 Excel 업로드

# 또는 데이터베이스 백업에서 복원
docker-compose exec -T db psql -U boramsafety_user boramsafety_db < backup.sql
```

## ✅ 체크리스트

설정 완료 후 다음 항목을 확인하세요:

- [ ] 회사명이 친구 회사 이름으로 변경되었는가?
- [ ] 연락처 정보가 정확한가?
- [ ] 제품 데이터가 모두 업로드되었는가?
- [ ] 제품 이미지가 제대로 보이는가?
- [ ] 모바일에서도 잘 보이는가?
- [ ] 데이터 백업을 설정했는가?

## 📞 추가 도움말

더 자세한 정보는 다음 문서를 참조하세요:

- [Excel 사용 가이드](EXCEL-GUIDE.md)
- [배포 가이드](DEPLOYMENT.md)
- [API 문서](http://localhost:8000/docs)

---

**축하합니다! 🎉**
친구의 안전용품 회사 웹사이트가 준비되었습니다!
