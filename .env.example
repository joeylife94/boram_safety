# ==========================================
# 보람안전물산(주) 환경 변수 설정
# ==========================================
# 
# 이 파일을 복사하여 .env 파일을 생성하세요:
# cp .env.example .env
#
# ⚠️ 주의: .env 파일은 절대 Git에 커밋하지 마세요!
#

# ==========================================
# Database Configuration (필수)
# ==========================================
DB_USER=postgres
DB_PASSWORD=your_secure_password_here_change_this
DB_HOST=localhost
DB_PORT=5432
DB_NAME=boram_safety

# Docker 사용 시 DB_HOST를 'db'로 변경:
# DB_HOST=db

# ==========================================
# Backend Configuration
# ==========================================
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# ==========================================
# Frontend Configuration
# ==========================================
# 프론트엔드 URL (프로덕션에서는 실제 도메인으로 변경)
FRONTEND_URL=http://localhost:3000

# Next.js Public API URL (프론트엔드에서 사용)
NEXT_PUBLIC_API_URL=http://localhost:8000/api

# ==========================================
# CORS Configuration
# ==========================================
# CORS 허용 오리진 (프로덕션에서는 쉼표로 구분하여 여러 도메인 설정)
# 개발 환경에서는 자동으로 localhost:3000, localhost:3001이 허용됩니다
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# ==========================================
# File Upload Configuration
# ==========================================
# 이미지 업로드 디렉토리 (상대 경로)
UPLOAD_DIR=../frontend/public/images

# 최대 파일 크기 (MB)
MAX_FILE_SIZE=10

# ==========================================
# Environment
# ==========================================
# 환경 설정: development, production, test
ENVIRONMENT=development

# ==========================================
# Security (프로덕션 필수)
# ==========================================
# JWT Secret Key (프로덕션에서는 강력한 랜덤 문자열 사용)
# SECRET_KEY=your_super_secret_key_here_at_least_32_characters

# Admin 비밀번호 (초기 설정용, 나중에 변경 필수)
# ADMIN_PASSWORD=change_this_password

# ==========================================
# Logging
# ==========================================
# 로그 레벨: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# 로그 파일 저장 경로
LOG_DIR=logs

# ==========================================
# Production Only (프로덕션 배포 시)
# ==========================================
# HTTPS 사용 여부
# USE_HTTPS=true

# 도메인
# DOMAIN=yourdomain.com

# SSL 인증서 경로
# SSL_CERT_PATH=/path/to/cert.pem
# SSL_KEY_PATH=/path/to/key.pem

# ==========================================
# 프로덕션 환경 예시
# ==========================================
# DB_HOST=your-db-host.com
# DB_PORT=5432
# FRONTEND_URL=https://yourdomain.com
# NEXT_PUBLIC_API_URL=https://api.yourdomain.com/api
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
# ENVIRONMENT=production
# USE_HTTPS=true

